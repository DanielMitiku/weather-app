import './style.css';

const fetchWeather = (location) => {
    let loc = location;
    fetch('url.url.com/api', { mode: 'cors'}).then(function(response){
        
    }).catch(function(err){

    });
};